apiVersion: v1
kind: Service
metadata:
  name: {{ include "maya.fullname" . }}
  labels: {{- include "maya.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: {{ include "maya.fullname" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
