<?xml version="1.0" encoding="UTF-8"?>
<!--
  MAYA — Model & AI Lifecycle Assurance
  Copyright © 2026 Ashutosh Sinha <ajsinha@gmail.com>. All rights reserved.
  Proprietary and confidential. See LICENSE and NOTICE at the repository root.

  No runtime dependencies, and that is deliberate rather than minimal.

  A governance platform that cannot be deployed air-gapped is one somebody works
  around, and an SDK with a dependency tree moves that problem into the client's
  build rather than solving it. java.net.http has been in the JDK since 11 and
  the JSON this client needs is small enough to read and write by hand — which
  is a smaller cost than asking a bank's platform team to get Jackson through
  their approval process to call a register.

  The test dependency is the exception and only reaches the test classpath.
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.maya</groupId>
  <artifactId>maya-sdk</artifactId>
  <version>0.1.0</version>
  <packaging>jar</packaging>

  <name>MAYA Java SDK</name>
  <description>A client for the MAYA register. It decides nothing.</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>5.10.2</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.14.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>3.5.2</version>
      </plugin>
    </plugins>
  </build>
</project>
