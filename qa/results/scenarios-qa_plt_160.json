{
 "summary": {
  "total": 1,
  "pass": 0,
  "fail": 1,
  "blocked": 0
 },
 "seconds": 1.9,
 "results": [
  {
   "id": "QA-PLT-160",
   "title": "Ten concurrent reads of a `max_reads: 1` share",
   "verdict": "FAIL",
   "evidence": "a share capped at one read served 5 of ten simultaneous reads at worst across 5 rounds (served [1, 5, 5, 5, 5]). `open_share` reads `share['reads']` before the state check and writes `reads + 1` after serving, with no lock and no conditional update \u2014 racers that read the same count all pass the cap and all serve. `max_reads` is the control a firm relies on when a link has gone somewhere it should "
  }
 ]
}